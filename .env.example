# Database (required)
DATABASE_URL=postgresql://USER:PASS@HOST/DB?sslmode=require

# Mailgun (optional for email alerts)
MAILGUN_DOMAIN=mg.example.com
MAILGUN_API_KEY=key-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
MAILGUN_FROM_NAME=Patent Scout Alerts
MAILGUN_FROM_EMAIL=alerts@example.com
MAILGUN_BASE_URL=https://api.mailgun.net/v3

# OpenAI (optional for embeddings; otherwise we use a deterministic hash fallback)
OPENAI_API_KEY=sk-...